{
  "name": "@wco/auth",
  "version": "1.0.0",
  "description": "WCO authentication & authorization — JWT, refresh rotation, RBAC guards, API keys",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\" --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test:unit": "jest --passWithNoTests"
  },
  "dependencies": {
    "@nestjs/common": "^10.2.10",
    "@nestjs/core": "^10.2.10",
    "@nestjs/jwt": "^10.2.0",
    "@nestjs/passport": "^10.0.3",
    "@wco/shared": "1.0.0",
    "@prisma/client": "^5.6.0",
    "argon2": "^0.31.2",
    "ioredis": "^5.3.2",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1"
  },
  "devDependencies": {
    "@types/passport-jwt": "^3.0.13",
    "typescript": "^5.3.0"
  }
}
