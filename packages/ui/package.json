{
  "name": "@wco/ui",
  "version": "1.0.0",
  "description": "WCO web UI primitives — shared tokens + components",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.{ts,tsx}\" --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test:unit": "jest --passWithNoTests"
  },
  "dependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.42",
    "@testing-library/react": "^14.1.2",
    "typescript": "^5.3.0"
  }
}
