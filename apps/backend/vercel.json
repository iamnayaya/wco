{
  "buildCommand": "npx prisma generate --schema ../../packages/database/prisma/schema.prisma && npm run build && npx prisma migrate deploy --schema ../../packages/database/prisma/schema.prisma && npx tsx ../../packages/database/prisma/seed.ts",
  "installCommand": "npm install --legacy-peer-deps --no-audit --no-fund",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}