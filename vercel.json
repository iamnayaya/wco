{
  "buildCommand": "npm run vercel:build:backend",
  "installCommand": "npm install --legacy-peer-deps --no-audit --no-fund --include=dev",
  "outputDirectory": "apps/backend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}